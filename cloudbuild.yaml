steps:
  # Build Docker image
  - name: 'gcr.io/cloud-builders/docker'
    id: build
    args:
      - 'build'
      - '-t'
      - 'us-central1-docker.pkg.dev/$PROJECT_ID/bolo-repo/web:$COMMIT_SHA'
      - '-t'
      - 'us-central1-docker.pkg.dev/$PROJECT_ID/bolo-repo/web:latest'
      - '--build-arg'
      - 'DATABASE_URL=$_DATABASE_URL'
      - '.'

  # Push to Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    id: push
    args:
      - 'push'
      - '--all-tags'
      - 'us-central1-docker.pkg.dev/$PROJECT_ID/bolo-repo/web'

  # Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    id: deploy
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'bomed-web'
      - '--image'
      - 'us-central1-docker.pkg.dev/$PROJECT_ID/bolo-repo/web:$COMMIT_SHA'
      - '--region'
      - 'us-central1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--set-env-vars'
      - 'DATABASE_URL=$_DATABASE_URL,NODE_ENV=production,BOLO_API_URL=$_BOLO_API_URL,BOLO_API_KEY=$_BOLO_API_KEY'
      - '--port'
      - '8080'
      - '--memory'
      - '512Mi'
      - '--cpu'
      - '1'
      - '--min-instances'
      - '0'
      - '--max-instances'
      - '3'

options:
  logging: CLOUD_LOGGING_ONLY

substitutions:
  _DATABASE_URL: ''
  _BOLO_API_URL: 'https://api.bolospot.com'
  _BOLO_API_KEY: ''
